<!-- SPDX-License-Identifier: LicenseRef-OpenSpace-AgentPrompts-Restricted -->

@AGENTS.md

## Codex

- Use `.agents/skills/` as the canonical project skill set for this repo.
- Use `.codex/agents/` for explorer/worker/reviewer roles and `.codex/rules/` for command policy.
- Prefer `_SD` for Space Dream fork-only content; extend inherited trees such as `_DV`, `Corvax`, or `Nyanotrasen` when they already own the feature.
- If a nearer subtree `AGENTS.md` exists for the files you are touching, treat it as additional scoped guidance.
